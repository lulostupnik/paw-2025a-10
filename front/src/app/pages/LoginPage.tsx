import { Link } from "react-router-dom";

export default function LoginPage() {
    return (
        <div>
            <h1>Login</h1>
            <p>Later we’ll add the real login form.</p>
            <Link to="/">Go to app</Link>
        </div>
    );
}
